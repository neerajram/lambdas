# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4


    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ github.event.inputs.name }}, job will start soon.."
            
  Install:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Install
      run: echo "installing Python..."
      
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: 3.7

    - name: Install Databricks cli
      run: |
        python --version
        pip --version
        python -m pip install --upgrade pip
        pip --version
        pip install databricks-cli
        echo "Installed Databricks CLI"
  
    - name: Configure Databricks CLI
      run: |
        databricks --version
        databricks workspace ls
      env:
        DATABRICKS_HOST: https://adb-7700374410621751.11.azuredatabricks.net/
        DATABRICKS_TOKEN: dapia3bf654dfbbe4f2ba5a19d010de65cd0



